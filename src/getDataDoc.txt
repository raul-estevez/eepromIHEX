Los parametros son:
    start_byte: El byte por el que queremos empezar a escribit
    data_lenght_bytes: El numero de bytes que queremos escribir
    data_array: El array donde se van a escribir los datos
    data_file: El archivo de donde se recogen los datos

El procedimiento es el siguiente:

    Primero se abre el archivo de datos en modo lectura. Se inician dos 
variables, data_counter (indica numericamente en que caracter estamos) 
y c (el caracter en el que estamos).
    El while coge un nuevo caracter y comprueba que no sea EOF (end of
file), si es asi se comprueba que el caracter en el que estamos sea 
posterior o igual al que le indicamos que fuera el primero. Esto es cierto
si data_couter es mayor o igual que el caracter numero 3*start_byte (Esto
es asi ya que el primer caracter hex de un byte esta siempre en la 
posicion multiplo de tres de su posicion en la lista, es decir, si queremo
comezar por el tercer byte, tendremos que empezar a escribir a partir del 
la posicion 3*3=9)
    
